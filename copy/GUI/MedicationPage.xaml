<Page x:Class="MediProject_Client.GUI.MedicationPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="">

    <Grid Margin="20">
        <Grid.Resources>
            <Style TargetType="Button" x:Key="MyButtonStyle">
                <Setter Property="Width" Value="120"/>
                <Setter Property="Height" Value="40"/>
                <Setter Property="Margin" Value="5"/>
                <Setter Property="FontWeight" Value="Bold"/>
                <Setter Property="Foreground" Value="White"/>
                <Setter Property="BorderThickness" Value="0"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border CornerRadius="8" Background="{TemplateBinding Background}">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
        </Grid.Resources>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="פרטי חומר פעיל" FontSize="24" FontWeight="Normal" Margin="0,0,0,20" HorizontalAlignment="Center"/>

        <DataGrid x:Name="MedicationGrid" 
                  Grid.Row="1" 
                  MaxHeight="200"
                  AutoGenerateColumns="False" 
                  CanUserAddRows="False" 
                  IsReadOnly="True"
                  AlternatingRowBackground="LightGray"
                  GridLinesVisibility="Horizontal"
                  BorderBrush="Gray"
                  BorderThickness="1">
            <DataGrid.Columns>
                <DataGridTextColumn Header="חומר פעיל" Binding="{Binding SubstanceName}" Width="150"/>
                <DataGridTextColumn Header="תיאור" Binding="{Binding Description}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>

        <Button Grid.Row="3" 
                Content="חזרה לאיזור האישי" 
                Style="{StaticResource MyButtonStyle}" 
                Background="Gray" 
                Click="Back_Click" 
                HorizontalAlignment="Center" 
                Margin="0,20,0,0"/>
    </Grid>
</Page>

<Page x:Class="MediProject_Client.GUI.PurchaseListPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:MediProject_Client.GUI"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      FlowDirection="RightToLeft"
      Title="MedicationsList">

    <Grid Background="WhiteSmoke">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.Margin>20</Grid.Margin>

        <TextBlock Grid.Row="0" Text="הקניות שלי" FontSize="20" FontWeight="Normal" Margin="0,0,0,10" Foreground="#333"/>

        <TextBox Grid.Row="1" Name="txtSearch" Height="35" Margin="0,0,0,15" 
                 Padding="10,5" VerticalContentAlignment="Center" FontSize="14" 
                 TextChanged="TxtSearch_TextChanged" BorderBrush="#CCC"/>

        <ListView Grid.Row="2" Name="listView" BorderThickness="0" Background="Transparent">
            <ListView.View>
                <GridView>
                    <GridView.ColumnHeaderContainerStyle>
                        <Style TargetType="GridViewColumnHeader">
                            <Setter Property="FontWeight" Value="Normal"/>
                        </Style>
                    </GridView.ColumnHeaderContainerStyle>

                    <GridViewColumn Header="שם תרופה" DisplayMemberBinding="{Binding medication.OriginalName}" Width="120"/>
                    <GridViewColumn Header="תאריך קניה" DisplayMemberBinding="{Binding PurchaseDate, StringFormat='{}{0:dd/MM/yyyy}'}" Width="110"/>
                    <GridViewColumn Header="כדורים ליום" DisplayMemberBinding="{Binding PillsPerDay}" Width="100"/>
                    <GridViewColumn Header="סה״כ כדורים" DisplayMemberBinding="{Binding PillAmount}" Width="100"/>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</Page>
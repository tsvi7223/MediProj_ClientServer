<Page x:Class="MediProject_Client.GUI.AddMedicationPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:MediProject_Client.GUI"
      mc:Ignorable="d" 
      d:DesignHeight="700" d:DesignWidth="800"
      Title="הוספת תרופה"
      Background="#F5F7FA">

    <Page.Resources>
        <Style TargetType="Label">
            <Setter Property="Foreground" Value="#4A5568"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Margin" Value="0,0,0,4"/>
            <Setter Property="HorizontalAlignment" Value="Right"/>
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="Height" Value="35"/>
            <Setter Property="Padding" Value="8,5"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="BorderBrush" Value="#CBD5E0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="White"/>
            <Style.Resources>
                <Style TargetType="Border">
                    <Setter Property="CornerRadius" Value="6"/>
                </Style>
            </Style.Resources>
        </Style>

        <Style TargetType="DatePicker">
            <Setter Property="Height" Value="35"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="BorderBrush" Value="#CBD5E0"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>
    </Page.Resources>

    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" 
                   Text="הוספת תרופה חדשה למערכת" 
                   FontSize="22" 
                   FontWeight="Bold" 
                   Foreground="#2D3748" 
                   HorizontalAlignment="Right" 
                   Margin="0,0,0,25"/>

        <StackPanel Grid.Row="1" FlowDirection="RightToLeft">

            <Label Content="תרופה:"/>
            <Grid Margin="0,0,0,15">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBox Name="txtMedicationName" 
                         IsReadOnly="True" 
                         Background="#EDF2F7" 
                         />

                <Button Grid.Column="1" 
                        Content="בחר..." 
                        Width="70" 
                        Margin="8,0,0,0"
                        Background="#4A90E2" 
                        Foreground="White" 
                        FontWeight="Bold"
                        BorderThickness="0"
                        Click="SelectMedication_Click">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="6"/>
                        </Style>
                    </Button.Resources>
                </Button>
            </Grid>

            <Label Content="מספר כדורים בחפיסה / מרשם:"/>
            <TextBox Name="txtPillsCount" 
                     Margin="0,0,0,15" 
                     InputScope="Number"/>
            <Label Content="תאריך הוצאת מרשם:"/>
            <DatePicker Name="dpPrescriptionDate" Margin="0,0,0,15"/>

            <Label Content="תאריך תחילת תוקף מרשם:"/>
            <DatePicker Name="dpStartDate" Margin="0,0,0,15"/>

            <Label Content="תאריך תפוגת מרשם:"/>
            <DatePicker Name="dpExpiryDate" Margin="0,0,0,15"/>

        </StackPanel>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Left" Margin="0,15,0,0">

            <Button Content="שמור תרופה" 
                    Width="120" 
                    Height="38" 
                    Background="#2ECC71" 
                    Foreground="White" 
                    FontWeight="Bold" 
                    BorderThickness="0"
                    Cursor="Hand"
                    Click="SaveButton_Click" Margin="0,0,10,0">
                <Button.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="6"/>
                    </Style>
                </Button.Resources>
            </Button>

            <Button Content="ביטול" 
                    Width="90" 
                    Height="38" 
                    Background="#E2E8F0" 
                    Foreground="#4A5568" 
                    FontWeight="SemiBold" 
                    BorderThickness="1"
                    BorderBrush="#CBD5E0"
                    Cursor="Hand"
                    Click="CancelButton_Click">
                <Button.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="6"/>
                    </Style>
                </Button.Resources>
            </Button>

        </StackPanel>
    </Grid>
</Page>
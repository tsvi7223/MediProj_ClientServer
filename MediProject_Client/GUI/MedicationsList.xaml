<Page x:Class="MediProject_Client.GUI.MedicationsList"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:MediProject_Client.GUI"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="MedicationsList">

    <Grid>
        <Grid>
            <ListView Name="listView">
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="Medication Name" Width="150">
                            <GridViewColumn.DisplayMemberBinding>
                                <Binding Path="OriginaLastName"/>
                            </GridViewColumn.DisplayMemberBinding>
                        </GridViewColumn>

                        <GridViewColumn Header="Price" DisplayMemberBinding="{Binding OrginalName}" Width="80"/>

                        <GridViewColumn Header="Description" DisplayMemberBinding="{Binding AvailableInIsrael}" Width="200"/>

                        <GridViewColumn Header="Category ID" DisplayMemberBinding="{Binding InHealthBox}" Width="100"/>

                        <!-- עמודת כפתור -->
                        <GridViewColumn Header="חלופות" Width="120">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <Button Content="הצג חלופות"
                                    Click="ShowAlternatives_Click"/>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>

                    </GridView>
                </ListView.View>
            </ListView>
        </Grid>
    </Grid>
</Page>

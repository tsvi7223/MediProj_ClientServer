<Page x:Class="MediProject_Client.GUI.AdminAreaPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d"
      Title="Pharmacy Care - AdminArea" Height="750" Width="550">

    <Page.Resources>
        <Style x:Key="WatermarkStyle" TargetType="TextBox">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="Bd" BorderBrush="#D1D1D1" BorderThickness="1" CornerRadius="4" Background="White" Height="30">
                            <Grid>
                                <ScrollViewer x:Name="PART_ContentHost" Margin="5,0,0,0" VerticalAlignment="Center"/>
                                <TextBlock x:Name="WatermarkText" Text="{TemplateBinding Tag}" Foreground="Gray" IsHitTestVisible="False" Margin="7,0,0,0" VerticalAlignment="Center" Visibility="Collapsed"/>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="Text" Value="">
                                <Setter TargetName="WatermarkText" Property="Visibility" Value="Visible"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Page.Resources>

    <Border CornerRadius="20" Background="White" Margin="20">
        <Border.Effect>
            <DropShadowEffect BlurRadius="15" Direction="0" ShadowDepth="0" Opacity="0.2"/>
        </Border.Effect>

        <Grid Margin="30">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" HorizontalAlignment="Center" Margin="0,10,0,0">
                <Border Width="100" Height="45" CornerRadius="22.5" BorderBrush="LightGray" BorderThickness="1">
                    <Border.Background>
                        <LinearGradientBrush StartPoint="0,0.5" EndPoint="1,0.5">
                            <GradientStop Color="DodgerBlue" Offset="0.5"/>
                            <GradientStop Color="MediumSeaGreen" Offset="0.5"/>
                        </LinearGradientBrush>
                    </Border.Background>
                </Border>

                <TextBlock Text="Pharmacy Care" FontSize="28" FontWeight="Bold" Foreground="DimGray" HorizontalAlignment="Center" Margin="0,10,0,0"/>
                <TextBlock Text="איזור ניהול:" FontSize="18" Foreground="#666" HorizontalAlignment="Center" Margin="0,5,0,0" FlowDirection="RightToLeft"/>
                <TextBlock Text="{Binding UserName, StringFormat='שלום לך, {0}'}" FontSize="15" FontWeight="SemiBold" Foreground="Black" HorizontalAlignment="Center" Margin="0,5,0,5" FlowDirection="RightToLeft"/>
            </StackPanel>

            <Grid Grid.Row="1" Margin="0,10,0,0" FlowDirection="RightToLeft">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Margin="0,0,0,15">
                    <TextBlock Text="הוספת תרופה חדשה למאגר" FontWeight="Bold" Foreground="LightSteelBlue" Margin="0,0,0,8"/>
                    <Grid Margin="0,0,0,5">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="2*"/>
                            <ColumnDefinition Width="1.2*"/>
                        </Grid.ColumnDefinitions>
                        <TextBox x:Name="txtMedName" Grid.Column="0" Style="{StaticResource WatermarkStyle}" Tag="הכנס שם תרופה"/>
                        <TextBox x:Name="txtSubstanceId" Grid.Column="1" Style="{StaticResource WatermarkStyle}" Tag="קוד חומר פעיל"/>
                    </Grid>

                    <Grid Margin="0,3,0,10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="100"/>
                        </Grid.ColumnDefinitions>
                        <CheckBox x:Name="chkInHealthBox" Grid.Column="0" Content="בסל הבריאות" VerticalAlignment="Center"/>
                        <CheckBox x:Name="chkAvailableInIsr" Grid.Column="1" Content="זמינה בישראל" VerticalAlignment="Center"/>
                        <Button x:Name="btnAddMed" Grid.Column="2" Content="הוסף" Height="30" Background="DodgerBlue" Foreground="White" FontWeight="Bold" Click="BtnAddMed_Click"/>
                    </Grid>
                </StackPanel>

                <StackPanel Grid.Row="1" Margin="0,0,0,15">
                    <TextBlock Text="מחיקת תרופה מהמערכת" FontWeight="Bold" Foreground="IndianRed" Margin="0,0,0,5"/>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="100"/>
                        </Grid.ColumnDefinitions>
                        <TextBox x:Name="txtDeleteMedName" Grid.Column="0" Style="{StaticResource WatermarkStyle}" Tag="הכנס קוד התרופה למחיקה"/>
                        <Button x:Name="btnDeleteMed" Grid.Column="1" Content="מחק" Background="IndianRed" Foreground="White" FontWeight="Bold" Click="BtnDeleteMed_Click"/>
                    </Grid>
                </StackPanel>

                <Button x:Name="btnLoadUsers" Grid.Row="2" Content="שלוף רשימת משתמשים" Height="35" Background="MediumSeaGreen" Foreground="White" FontWeight="Bold" Margin="0,0,0,10" Click="BtnLoadUsers_Click"/>

                <DataGrid x:Name="dgUsers" Grid.Row="3" AutoGenerateColumns="False" IsReadOnly="True" Background="WhiteSmoke" BorderBrush="#E0E0E0" RowBackground="White" AlternatingRowBackground="WhiteSmoke">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="קוד" Binding="{Binding ID}" Width="50"/>
                        <DataGridTextColumn Header="שם משתמש" Binding="{Binding UserName}" Width="*"/>
                        <DataGridTextColumn Header="קופת חולים" Binding="{Binding Kupa.Name}" Width="*"/>
                        <DataGridTextColumn Header="מנהל" Binding="{Binding IsAdmin}" Width="60"/>
                    </DataGrid.Columns>
                </DataGrid>

                <Button x:Name="btnLogout" Grid.Row="4" Content="התנתקות" Height="35" Background="IndianRed" Foreground="White" FontWeight="Bold" Margin="0,15,0,0" Click="Logout_Click"/>
            </Grid>
        </Grid>
    </Border>
</Page>